apiVersion: v1
kind: Service
metadata:
  name: {{ include "ask-docs.fullname" . }}
  labels:
    {{- include "ask-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ask-docs.selectorLabels" . | nindent 4 }}
