apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ask-docs.fullname" . }}-test-connection"
  labels:
    {{- include "ask-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ask-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
